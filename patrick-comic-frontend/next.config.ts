/** @type {import('next').NextConfig} */
const nextConfig = {
  // Đưa nó ra ngoài như thế này, KHÔNG để trong experimental nữa
  allowedDevOrigins: ['patrick-comic'], 
  
  // Các cấu hình khác nếu có...
};

export default nextConfig;